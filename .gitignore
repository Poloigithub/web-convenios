__pycache__/
*.pyc
.DS_Store
_site/
tailwindcss

# Credenciales: NUNCA en el repositorio (van en Secrets de GitHub)
config.env
*.env
!web/*.env

# La BBDD se reconstruye desde convenios.ndjson (git no deltea binarios)
datos/convenios.sqlite

# Generado en cada pasada (lleva marca de tiempo): conflictaría siempre.
# El dato completo está en convenios.ndjson; el JSON se publica en la web.
datos/convenios.json
